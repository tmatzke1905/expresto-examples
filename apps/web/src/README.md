# Web Workspace Structure

This folder will host the React application code.

## Planned subfolders

- `app/`: app shell, routes, and layout
- `components/`: reusable UI building blocks
- `features/`: feature-specific UI modules
- `lib/`: browser-side helpers, API clients, and state utilities
- `styles/`: global styling tokens and layout rules
