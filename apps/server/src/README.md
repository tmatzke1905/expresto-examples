# Server Workspace Structure

This folder will host the expresto-server application code.

## Planned subfolders

- `controllers/`: feature endpoints and demo APIs
- `hooks/`: lifecycle hook registrations
- `jobs/`: scheduler jobs
- `lib/`: server-side helpers and adapters
- `services/`: runtime services registered in the ServiceRegistry
